[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://REPLACE_WITH_YOUR_BACKEND_URL/api/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
